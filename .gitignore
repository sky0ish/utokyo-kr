# ── 화이트리스트 방식: 아래 명시한 것 외에는 전부 제외 ──
# (자료 폴더 "1. 동경대 총동문회"의 개인정보가 절대 올라가지 않도록)
/*

# 홈페이지 파일만 허용
!/index.html
!/YB/
!/OB/
!/images/
!/auth/
!/board/
!/admin/
!/supabase/
!/common/

# 페이스북 이전 글(개인 이름 포함)은 공개 저장소에 올리지 않음
board/fb_import_*.sql
board/roster_import.sql

# 저장소 관리 파일 허용
!/.gitignore
!/README.md
!/CNAME
